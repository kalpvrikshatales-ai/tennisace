# Backend API URL - set to live Render URL in production
NEXT_PUBLIC_API_URL=http://localhost:8000

# Web Push VAPID public key (used by browser for notifications)
NEXT_PUBLIC_VAPID_KEY=your_public_vapid_key
